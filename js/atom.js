var scene=new THREE.Scene();var aspectRatio=window.innerWidth/window.innerHeight;var camera=new THREE.PerspectiveCamera(75,aspectRatio,1,10000);camera.position.z=350;scene.add(camera);directionalLight=new THREE.DirectionalLight(0xffffff,0.5);directionalLight.position.set(1,1,2).normalize();var shape=new THREE.SphereGeometry(75,20,20);var texture1=new THREE.MeshBasicMaterial({map:THREE.ImageUtils.loadTexture('img/black_bg.png')});var nucleus=new THREE.Mesh(shape,texture1);scene.add(nucleus);var electronShape=new THREE.SphereGeometry(30,20,20);var electron1=new THREE.Mesh(electronShape,texture1);scene.add(electron1);electron1.position.set(-150,150,0);var electron2=new THREE.Mesh(electronShape,texture1);scene.add(electron2);electron2.position.set(150,150,0);var electron3=new THREE.Mesh(electronShape,texture1);scene.add(electron3);electron3.position.set(0,0,150);var renderer=new THREE.WebGLRenderer({alpha:true,antialias:true});renderer.setSize(window.innerWidth*0.30,window.innerHeight*0.30);document.querySelector(".atom").appendChild(renderer.domElement);var clock=new THREE.Clock();function animate(){requestAnimationFrame(animate);var t=clock.getElapsedTime();renderer.render(scene,camera);electron1.position.x=Math.sin(2*t)*-150;electron1.position.y=Math.sin(2*t)*150;electron1.position.z=Math.cos(2*t)*150;electron2.position.x=Math.cos(2*t)*150;electron2.position.y=Math.cos(2*t)*150;electron2.position.z=Math.sin(2*t)*150;var tOffset=1.5+clock.getElapsedTime();electron3.position.x=Math.sin(2*tOffset)*0;electron3.position.y=Math.sin(2*tOffset)*150;electron3.position.z=Math.cos(2*tOffset)*150;}animate();